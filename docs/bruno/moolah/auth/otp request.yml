info:
  name: otp request
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/v1/auth/otp/request
  body:
    type: json
    data: |-
      {
        "email": "a@a.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
