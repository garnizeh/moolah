info:
  name: otp verification
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/v1/auth/otp/verify"
  body:
    type: json
    data: |-
      {
        "email": "a@a.com",
        "codw": "122"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
