info:
  name: otp verification
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/v1/auth/otp/verify
  body:
    type: json
    data: |-
      {
        "email": "a@a.com",
        "code": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
