# Task 1.6.5 — Generate Swagger Documentation

> **Roadmap Ref:** Phase 1 — MVP › 1.6 Quality Gate
> **Status:** ✅ `done`
> **Last Updated:** 2026-03-10
> **Assignee:** GitHub Copilot
> **Estimated Effort:** M

---

## 1. Summary

Implement automated API documentation using Swaggo (Swagger/OpenAPI). This includes adding annotations to handlers, generating the documentation, adding a `Makefile` rule, and ensuring the documentation is verified in the CI pipeline.

---

## 2. Context & Motivation

Consistent and up-to-date API documentation is critical for frontend development and external integrations. By using Swaggo, we can generate OpenAPI specifications directly from Go code annotations, keeping docs in sync with implementation.

---

## 3. Scope

### In scope

- [x] Add basic Swaggo annotations to `cmd/api/main.go` and existing handlers in `internal/handler/`.
- [x] Implement `make swagger` rule to generate `api/` (changed from `docs/swagger/`).
- [x] Add a CI step (`make swagger-check`) to verify that `swag init` doesn't produce uncommitted changes.
- [x] Serve Swagger UI via `/swagger/` route.

---

## 4. Technical Design

### Files to create / modify

| Action | Path | Purpose |
| ------ | ---- | ------- |
| MODIFY | `Makefile` | Add `swagger` and `swagger-check` rules |
| MODIFY | `cmd/api/main.go` | Add General API annotations |
| MODIFY | `internal/handler/*.go` | Add operation annotations to handlers |
| CREATE | `api/` | Generated Swagger files (swagger.json, docs.go, etc.) |
| MODIFY | `internal/server/routes.go` | Register `/swagger/` route |

---

## 5. Acceptance Criteria

- [x] `make swagger` generates valid OpenAPI files in `api/`.
- [x] All Phase 1 handlers have complete annotations (Summary, Tags, Responses, Param).
- [x] `make task-check` (CI simulation) fails if Swagger docs are out of date.
- [x] Swagger UI is accessible at `/swagger/`.
- [x] `golangci-lint` passes (autogenerated code excluded).

---

## 9. Change Log

| Date       | Author | Change                    |
| ---------- | ------ | ------------------------- |
| 2026-03-08 | —      | Task created from user request |
| 2026-03-09 | Copilot| Annotations added, Makefile updated, route registered |
| 2026-03-10 | Copilot| Marked as done |
